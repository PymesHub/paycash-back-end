CREATE TABLE users (
    id CHAR(36) PRIMARY KEY,
    name VARCHAR(255),
    lastName VARCHAR(255),
    email VARCHAR(255) UNIQUE,
    birthday DATE,
    genre VARCHAR(10),
    statusPLD VARCHAR(50) NULL
);