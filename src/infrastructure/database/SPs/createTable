CREATE TABLE users (
    id CHAR(36) PRIMARY KEY UNIQUE,
    name VARCHAR(255),
    lastName VARCHAR(255),
    email VARCHAR(255),
    birthday VARCHAR(255),
    genre VARCHAR(10),
    statusPLD VARCHAR(50) NULL
);